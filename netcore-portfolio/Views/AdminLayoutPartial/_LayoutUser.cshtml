@if (User.Identity.IsAuthenticated)
{
    <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        <div class="image">
            <!-- Kullanılacak ViewData["UserPhoto"] --> 
            <img src="@ViewBag.IM" class="img-circle elevation-2" alt="User Image">
        </div>
        <div class="info">
            <a href="#" class="d-block">
                @ViewBag.KA
            </a>
        </div>
        <ul>
        </ul>
    </div>}

